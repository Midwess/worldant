# Worldant World

This is a blank Worldant world. Add executable functions under `commands/` and durable processes
under `workflows/`. The world directory name becomes the app namespace.

```bash
worldant build
worldant serve
```

Read the canonical docs in the public Worldant repository: `docs/worldant/`.

