# Migrations

Add ordered PostgreSQL migrations such as `0001_init.sql`. Never edit a migration after it has
been applied; add the next numbered file instead. An empty directory is valid for an app with no
database schema.

