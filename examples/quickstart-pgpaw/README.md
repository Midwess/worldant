# Quickstart PgPaw Schema

This PgPaw source root owns the application schema used by the neighboring Worldant quickstart.
Apply it with `pgpaw migrate`; do not move it into the Worldant source tree.
